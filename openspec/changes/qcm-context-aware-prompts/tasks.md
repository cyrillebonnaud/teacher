## 1. Prompts

- [x] 1.1 Ajouter les paramètres `subject` et `title` à `buildQcmPrompt` dans `lib/prompts.ts`
- [x] 1.2 Ajouter les paramètres `subject` et `title` à `buildFreeTopicQcmPrompt` dans `lib/prompts.ts`
- [x] 1.3 Intégrer `MATIÈRE` et `SÉQUENCE` dans le texte du prompt (les deux fonctions)

## 2. Actions

- [x] 2.1 Passer `sequence.subject` et `sequence.name` depuis `generateQcm` dans `actions/evaluations.ts`
